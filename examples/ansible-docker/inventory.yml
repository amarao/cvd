---
all:
  children:
    cvd_managed:
      vars:
        container_image: alpine:3.20
      hosts:
        web: {}
    webservers:
      vars:
        greeting: Hello from CVD
      hosts:
        web: {}
