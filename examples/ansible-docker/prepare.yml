---
- name: Prepare the containers for convergence
  hosts: webservers
  gather_facts: false
  tasks:
    - name: Check container access and a writable HTTP document root
      ansible.builtin.raw: mkdir -p /srv/www
      changed_when: false
