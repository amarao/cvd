---
- name: Configure the web containers through the runtime inventory
  hosts: webservers
  gather_facts: false
  tasks:
    - name: Configure the greeting served at the HTTP root
      # no python in the image
      ansible.builtin.raw: "printf '%s\\n' {{ greeting | quote }} > /srv/www/index.html"
      changed_when: true
